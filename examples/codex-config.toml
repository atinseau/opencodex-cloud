# Example only: the installer generates absolute paths for the current machine.
# Root keys must stay before the first [table].
model_provider = "opencodex_cloud"
model_catalog_json = "/Users/alice/.codex/opencodex-cloud-catalog.json"

[model_providers.opencodex_cloud]
name = "OpenCodex Cloud"
base_url = "https://ai.example.com/v1"
wire_api = "responses"

[model_providers.opencodex_cloud.auth]
command = "/Users/alice/.local/bin/opencodex-cloud"
args = ["auth-token"]
timeout_ms = 5000
refresh_interval_ms = 0
