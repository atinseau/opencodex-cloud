{
  "name": "opencodex-cloud-client",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "bin": {
    "opencodex-cloud": "src/cli.ts"
  },
  "scripts": {
    "check": "bunx tsc --noEmit",
    "test": "bun test",
    "build": "bun build --compile --minify --sourcemap=none src/cli.ts --outfile dist/opencodex-cloud"
  },
  "dependencies": {
    "@clack/prompts": "1.7.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.5",
    "typescript": "5.9.3"
  }
}
